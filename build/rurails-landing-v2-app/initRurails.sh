
gem install bundler:2.2.15
npm install
gem update --system 


#bundle install
#rake db:create && rake db:migrate && rake db:seed
echo "The app container is ready, now it goes on standby.. start the rails server from within :)"
#tail -f /dev/null
#rails s -b 0.0.0.0
